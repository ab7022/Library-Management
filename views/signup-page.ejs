<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script type="module" src="js/login.js" defer></script>
    <link rel="stylesheet" href="styles/login-page.css">
    <title>Library Sign Up</title>
</head>
<body>
    
    <section class="form animated flipInX">
        <h1>Welcome</h1>
        <h2>Signup Page</h2>
        <% if(inputData.hasError) {%>
            <p id="input-error2"><%= inputData.message %></p>
            <% } %>
        <form class="loginbox" autocomplete="off" action="/signup" method="POST">
          <input placeholder="Email" type="email" id="email" name="email" required value="<%= inputData.email %>"> 
          <input placeholder="Confirm Email" type="email" id="confirm-email" name="confirm-email" required value="<%= inputData.confirmEmail %>"> 
          <input placeholder="Password" type="password" id="password" name="password" required value="<%= inputData.password %>"> 
          <button id="submit" type="submit">Signup</button>
      </form>
      </section>
</body>
</html>